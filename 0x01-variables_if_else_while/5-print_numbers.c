#include <stdio.h>
/**
 * main - this is the main function
 *
 * Return: always 0
 */
int main(void)
{
int digit;

for (digit = 0; digit <= 9; digit++)
{
printf("%d", digit);
}
printf("\n");
return (0);
}
