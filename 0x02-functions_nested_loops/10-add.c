#include "main.h"
#include <stdio.h>

/**
 * add - function prints the result of two numbers
 * @i: first parameter
 * @j: second parameter
 *
 * Return: always i + j
 */


int add(int i, int j)
{
return (i + j);
}
