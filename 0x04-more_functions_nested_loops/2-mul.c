#include "main.h"
/**
 * mul - function that multiple to integer
 * @a : fist parameter
 * @b : second parameter
 *
 * Return: the result
 */

int mul(int a, int b)
{
int m;

m = a * b;
	return (m);
}
